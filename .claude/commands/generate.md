Generate a 3D printable model from the description: $ARGUMENTS

Run this command and show the output:
```bash
python print_farm_bot.py generate "$ARGUMENTS"
```

After it completes, tell the user:
- The model ID so they can reference it
- That they can type `/view <model_id>` to preview it in 3D
- That they can type `/queue add <model_id>` to queue it for printing
