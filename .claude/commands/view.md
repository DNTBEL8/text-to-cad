Open a 3D model in the viewer.

If $ARGUMENTS is empty, first run `python print_farm_bot.py list` and show the user their models so they can pick one.

If $ARGUMENTS is provided, run:
```bash
python print_farm_bot.py view $ARGUMENTS
```

The viewer will open in the browser. Tell the user the URL it prints.
